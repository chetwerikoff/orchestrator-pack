# Runtime hard-cut evidence

Issue #1352 owns the base-current census and final AO-free runtime inventory in this directory.

The implementation branch temporarily uses the existing heavy-topology artifact lane to export an exact tracked-source archive for connector-only execution. That bootstrap is removed before final evidence is published.
