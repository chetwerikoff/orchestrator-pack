# AO worker pipeline smoke probe (opk-probe)

Synthetic AO spawn target used to exercise the managed worker lifecycle end-to-end
(`ao acknowledge` → branch `feat/opk-probe` → PR → CI → `ready_for_review`).

This draft is a no-ceremony marker only; it is not queued implementation work.
