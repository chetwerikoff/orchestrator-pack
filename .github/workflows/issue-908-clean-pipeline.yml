name: issue-908-clean-pipeline

on:
  push:
    branches:
      - diag/issue-908-clean-pipeline
  pull_request:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Download failed finalizer log
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api "repos/${GITHUB_REPOSITORY}/actions/jobs/88139809701/logs" > "$RUNNER_TEMP/issue-908-finalizer.log"
          tail -n 300 "$RUNNER_TEMP/issue-908-finalizer.log"
      - uses: actions/upload-artifact@v4
        with:
          name: issue-908-finalizer-log
          path: ${{ runner.temp }}/issue-908-finalizer.log
          retention-days: 1
