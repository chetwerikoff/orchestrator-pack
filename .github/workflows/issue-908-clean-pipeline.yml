name: issue-908-clean-pipeline

on:
  push:
    branches:
      - diag/issue-908-clean-pipeline

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/scope-guard-pr-files-pagination
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Capture clean pipeline guard
        shell: bash
        run: |
          set +e
          pwsh -NoProfile -File ./scripts/check-ci-pipeline-split.ps1 > "$RUNNER_TEMP/issue-908-clean-pipeline.log" 2>&1
          printf '\nexit=%s\n' "$?" >> "$RUNNER_TEMP/issue-908-clean-pipeline.log"
          cat "$RUNNER_TEMP/issue-908-clean-pipeline.log"
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: issue-908-clean-pipeline
          path: ${{ runner.temp }}/issue-908-clean-pipeline.log
          retention-days: 1
