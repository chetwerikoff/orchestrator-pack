name: issue-941-base-window-cache-publisher-disabled

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: issue-941-base-window-cache-publisher
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
