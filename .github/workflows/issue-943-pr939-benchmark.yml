name: issue-943-pr939-benchmark-disabled

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: issue-943-pr939-benchmark-v2
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
