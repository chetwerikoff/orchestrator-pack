name: issue-943-policy-apply-disabled

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: issue-943-policy-apply
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
