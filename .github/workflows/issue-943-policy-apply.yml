name: issue-943-changed-first-index-publisher-disabled

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: issue-943-changed-first-index-publisher
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
