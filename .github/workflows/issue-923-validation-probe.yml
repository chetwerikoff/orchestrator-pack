name: issue-923-lane-one-stack

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  actions: read
  contents: read

jobs:
  extract:
    if: github.event.pull_request.head.ref == 'agent/issue-923-validation-probe'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      SOURCE_JOB_ID: '88704808829'
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Print lane-one assertion context
        shell: bash
        run: |
          set -euo pipefail
          api="https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/jobs/${SOURCE_JOB_ID}/logs"
          curl --fail --silent --show-error --location \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$api" > lane.log
          python3 - <<'PY'
          import re
          ansi = re.compile(r'\x1b\[[0-9;]*m')
          lines = [ansi.sub('', line) for line in open('lane.log', encoding='utf-8-sig', errors='replace').read().splitlines()]
          for index, line in enumerate(lines):
              if 'AssertionError: expected [] to have a length of 1 but got +0' not in line:
                  continue
              for cursor in range(max(0, index - 12), min(len(lines), index + 24)):
                  print(f'{cursor + 1}:{lines[cursor]}')
          PY
