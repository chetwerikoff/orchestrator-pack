---
description: Draw diagrams as plain ASCII art, never as mermaid blocks
alwaysApply: true
---

# Diagrams: ASCII art only

Draw diagrams as plain ASCII (`[Box]`, `-->`, `|`, `v`, `+---+`) inside an
ordinary fenced code block. Do not use a mermaid fence.
