# Fixture: quote-dense issue body (must not fail pack checks)

Config example: use `"key": "value"` and `--command "powershell.exe -File foo.ps1"`.
JSON: {"review": "failed", "note": "not a launch failure signal"}
