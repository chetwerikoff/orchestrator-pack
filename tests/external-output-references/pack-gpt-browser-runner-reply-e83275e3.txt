{"findings":[{"type":"quality","code":"gpt:strict-terminal-shape","severity":"blocking","path":"scripts/lib/pack-gpt-reviewer.ts","summary":"GPT output is accepted through the legacy lenient Codex normalizer, so malformed prose such as I could not inspect the PR. NO_FINDINGS is normalized to a clean terminal verdict; {\"findings\":[]} is also accepted and mapped into an internally inconsistent findings verdict. The concrete harm is that an unsuccessful/invalid browser reply can become merge-readiness evidence, directly violating #1031's requirement that malformed or internally inconsistent GPT output fail closed. FIX_NOW: make the GPT boundary accept only the exact NO_FINDINGS token or one complete top-level findings JSON object with a non-empty, fully valid gpt-browser findings array, rejecting wrappers, fences, surrounding prose, empty findings, and direct terminal-verdict JSON; add focused negative tests. This needs no persistent machinery.","source":"gpt-browser"},{"type":"test","code":"ac3:realistic-smoke-not-adapter-bound","severity":"blocking","path":"tests/external-output-references/pack-gpt-browser-live-chat-1935fe18.json","summary":"The retained AC3 live smoke is not a production GPT-adapter invocation: its captured operator prompt contains only the PR URL plus выполни пак ревью, while the artifact claiming the generated prompt contained the bound SHA, canonical contract, and transport rules was recorded separately later and is not causally tied to that browser turn; the non-mocked automated path uses the fixture npm shim rather than the real tracked browser result. Therefore the required runner/generated-prompt → tracked chatgpt-browser-turn → parseable adapter terminal-result boundary remains unproved, so a production integration defect could survive the green harness coverage. FIX_NOW: retain one real runner/adapter-driven tracked turn with its exact generated prompt, parseable terminal result, and runner-owned GitHub publication all bound to the same run/head. No new persistent machinery is needed.","source":"gpt-browser"}]}