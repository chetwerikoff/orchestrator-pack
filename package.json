{
  "name": "orchestrator-pack",
  "private": true,
  "type": "module",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "pretest": "node scripts/check-vitest-live-store-isolation.mjs",
    "test": "node scripts/run-vitest-with-harness.mjs run",
    "pretest:watch": "node scripts/check-vitest-live-store-isolation.mjs",
    "test:watch": "node scripts/run-vitest-with-harness.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10"
  }
}
