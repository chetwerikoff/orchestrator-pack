{
  "description": "Issue #589 spawn-shape guard baseline. Backtick documentation templates with angle-bracket placeholders (e.g. `ao spawn --claim-pr <PR>`) are skipped by isDocumentationSpawnTemplate; runnable line matches are always validated with no global command allowlist."
}
